services:
  backend:
    build:
      context: ./backend
    container_name: yolo-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: yolo-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
      